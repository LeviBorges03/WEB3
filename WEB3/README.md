# WEB3 HTML Mockup

Esta é a versão estática das Views para fins de prototipação da UI.

Aqui você encontra a interface reproduzida em HTML/CSS/JS puros.

## Sistema de Favoritos
O arquivo `Livro/index.html` já possui o sistema de mock de favoritos implementado em Javascript utilizando o localStorage com a chave `bibliotecaFavoritos`, da mesma forma que a aplicação principal.